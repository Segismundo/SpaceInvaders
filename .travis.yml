language: java
jdk: jre

before_cache:
- rm -f $HOME/.gradle/caches/modules-2.lock
cache:
  - "$HOME/.m2"
  - "$HOME/.gradle"
  - "$HOME/.gradle/wrapper/"
  - "$HOME/.gradle/caches/"
  - "$HOME/.gradle/local.propierties"
script:
  - ./gradlew build --scan -s
